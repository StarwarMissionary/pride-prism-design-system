# Source marker for the native desktop adapter.
# scripts/build-theme.mjs emits the actual palette-specific appearance snippet.
# Use dist/<palette>/adapters/chatgpt/pride-prism-config.toml; never replace a
# complete user config with an adapter snippet.
