# ChatGPT/Codex adapter

The native desktop appearance system supports separate light and dark chrome colors plus code themes. Merge `pride-prism-config.toml` into the user Codex configuration, then reopen the desktop app once so its main settings store reloads.

This adapter intentionally uses static, readable colors. The optional overlay is the only motion layer.
